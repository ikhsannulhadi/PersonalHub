<script setup lang="ts">import { onMounted } from 'vue'; import { site } from '@/config/site'; onMounted(() => { document.title = site.seo.title; const description = document.querySelector('meta[name="description"]') ?? document.head.appendChild(document.createElement('meta')); description.setAttribute('name', 'description'); description.setAttribute('content', site.seo.description) });</script><template><RouterView /></template>
